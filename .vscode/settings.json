{
  "liveServer.settings.https": {
    "enable": true, //set it true to enable the feature.
    "cert": "./localhost.crt", //full path of the certificate
    "key": "./localhost.key", //full path of the private key
    "passphrase": ""
  }
}
